import {StyleSheet} from 'react-native';
import {Icon} from '../../../theme';

const styles = StyleSheet.create({
  logoIcon: {
    height: Icon.defaultPlusHeight,
    width: Icon.defaultPlusWidth,
  },
});
export default styles;
